import { ActionDrawerCloseDirective } from './action-drawer-close.directive';

describe('ActionSheetCloseDirective', () => {
  it('should create an instance', () => {
	const directive = new ActionDrawerCloseDirective();
	expect(directive).toBeTruthy();
  });
});
