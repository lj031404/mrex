import { SubscriptionRequireDirective } from './subscription-require.directive';

describe('SubscriptionRequireDirective', () => {
  it('should create an instance', () => {
    const directive = new SubscriptionRequireDirective();
    expect(directive).toBeTruthy();
  });
});
