import { FormFieldLabelDirective } from './form-field-label.directive';

describe('FormFieldLabelDirective', () => {
  it('should create an instance', () => {
	const directive = new FormFieldLabelDirective();
	expect(directive).toBeTruthy();
  });
});
