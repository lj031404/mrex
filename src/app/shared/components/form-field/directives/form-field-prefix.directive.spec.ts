import { FormFieldPrefixDirective } from './form-field-prefix.directive';

describe('FormFieldPrefixDirective', () => {
  it('should create an instance', () => {
	const directive = new FormFieldPrefixDirective();
	expect(directive).toBeTruthy();
  });
});
