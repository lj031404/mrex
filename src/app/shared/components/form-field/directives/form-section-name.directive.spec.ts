import { FormSectionNameDirective } from './form-section-name.directive';

describe('FormSectionNameDirective', () => {
  it('should create an instance', () => {
	const directive = new FormSectionNameDirective();
	expect(directive).toBeTruthy();
  });
});
