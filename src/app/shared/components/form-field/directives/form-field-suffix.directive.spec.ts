import { FormFieldSuffixDirective } from './form-field-suffix.directive';

describe('FormFieldSuffixDirective', () => {
  it('should create an instance', () => {
	const directive = new FormFieldSuffixDirective();
	expect(directive).toBeTruthy();
  });
});
