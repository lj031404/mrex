import { NumericMaskCleanDirective } from './numeric-mask-clean.directive';

describe('NumericMaskCleanDirective', () => {
  it('should create an instance', () => {
	// const directive = new NumericMaskCleanDirective();
	// expect(directive).toBeTruthy();
  });
});
