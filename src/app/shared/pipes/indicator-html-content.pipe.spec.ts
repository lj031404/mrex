import { IndicatorHtmlContentPipe } from './indicator-html-content.pipe';

describe('IndicatorHtmlContentPipe', () => {
  it('create an instance', () => {
	const pipe = new IndicatorHtmlContentPipe();
	expect(pipe).toBeTruthy();
  });
});
